// //1
function numbers(num1,num2) {
    const M = Math.min(number.number1, number.number2)
    console.log(M)
}
 const number = {
    number1: prompt("num1?"),
    number2: prompt("num2?")
 };

numbers(number.number1, number.number2);

//2

function countChar() {
    const num = prompt('???')
    console.log(num.length)
}
countChar()
